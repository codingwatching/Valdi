name: Valdi CI

on:
  pull_request:
    paths:
      - '**/*.bzl'
      - '**/BUILD.bazel'
      - '**/BUILD'
      - 'MODULE.bazel'
      - 'registry/**'
      - 'tools/ci/**'
      - 'tools/api_surface/**'
      - 'npm_modules/cli/test/**'
      - '.github/workflows/bzl-changes.yml'
      - '**/*.cpp'
      - '**/*.hpp'
      - '**/*.h'
      - '**/*.c'
      - 'third-party/**'
      - 'src/**/*.ts'
      - 'src/**/*.tsx'
      - 'compiler/**'
      - 'apps/snapshot_tests/**'
      - 'snap_drawing/**'
  push:
    branches:
      - main
    paths:
      - '**/*.bzl'
      - '**/BUILD.bazel'
      - '**/BUILD'
      - 'MODULE.bazel'
      - 'registry/**'
      - 'tools/ci/**'
      - 'tools/api_surface/**'
      - 'npm_modules/cli/test/**'
      - '.github/workflows/bzl-changes.yml'
      - '**/*.cpp'
      - '**/*.hpp'
      - '**/*.h'
      - '**/*.c'
      - 'third-party/**'
      - 'src/**/*.ts'
      - 'src/**/*.tsx'
      - 'compiler/**'
      - 'apps/snapshot_tests/**'
      - 'snap_drawing/**'

# Workflow-level permissions: id-token: write needed for GCP WIF auth on push events.
# For fork PRs, GitHub automatically downgrades to read-only — the auth step
# has continue-on-error so builds proceed without remote cache.
permissions:
  contents: read
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke-test:
    name: Valdi Smoke Tests
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure Bazel cache
        uses: ./.github/actions/bazel-cache
        with:
          gcp_project_id: ${{ vars.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ vars.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
          cache_bucket: ${{ vars.BAZEL_CACHE_BUCKET }}
          cache_key: smoke-test

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Bazel and watchman
        run: |
          brew install bazelisk watchman
          bazel --version
          watchman --version

      - name: Install Valdi CLI
        run: |
          cd npm_modules/cli
          npm run cli:install

      - name: Run smoke tests
        run: ./tools/ci/bootstrap_app.sh
        env:
          OPEN_SOURCE_DIR: ${{ github.workspace }}
          PROJECT_ROOT: /tmp/valdi_app

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-macos
          path: |
            /tmp/valdi_app/*.log
            /tmp/valdi_app/config.yaml
            /tmp/valdi_app/WORKSPACE
            /tmp/valdi_app/.bazelrc
          retention-days: 5

  # Note: Linux CLI distribution tests moved to dedicated workflow:
  # .github/workflows/test-cli-linux.yml
  # This avoids duplication and provides better test organization.

  linux-ci:
    name: "Linux: ${{ matrix.name }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: C++ Tests
            task: test-cpp
          - name: Build Compiler
            task: build-compiler
          - name: Build & Export
            task: build-export

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Configure Bazel cache
        uses: ./.github/actions/bazel-cache
        with:
          gcp_project_id: ${{ vars.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ vars.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
          cache_bucket: ${{ vars.BAZEL_CACHE_BUCKET }}
          cache_key: ${{ matrix.task }}

      - name: Setup Linux environment
        run: source ./tools/ci/setup_linux_env.sh

      - name: Setup environment and install Valdi CLI
        if: matrix.task == 'build-export'
        run: |
          mkdir -p ~/.npm-global/lib
          npm config set prefix '~/.npm-global'
          npm install -g npm@11
          export PATH=~/.npm-global/bin:$PATH
          cd npm_modules/cli
          npm run cli:install
          valdi --help

      - name: "Validate: External build graph"
        if: matrix.task == 'test-cpp'
        run: ./tools/ci/test_external_build_graph.sh

      - name: "Run: C++ Tests"
        if: matrix.task == 'test-cpp'
        run: ./tools/ci/run_tests.sh

      - name: "Run: Build Compiler"
        if: matrix.task == 'build-compiler'
        run: bazel build //compiler/compiler:local_valdi_compiler --//bzl/valdi:use_local_compiler=true

      - name: "Run: Build core targets"
        if: matrix.task == 'build-export'
        run: |
          export PATH=~/.npm-global/bin:$PATH
          ./tools/ci/build_core_targets.sh
        env:
          OPEN_SOURCE_DIR: ${{ github.workspace }}
          PROJECT_ROOT: /tmp/valdi_app

      - name: Refresh Bazel cache auth (pre-test_exported_lib)
        if: matrix.task == 'build-export'
        uses: ./.github/actions/bazel-cache-refresh
        with:
          gcp_project_id: ${{ vars.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ vars.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: "Run: Test exported lib"
        if: matrix.task == 'build-export'
        run: |
          export PATH=~/.npm-global/bin:$PATH
          ./tools/ci/test_exported_lib.sh
        env:
          OPEN_SOURCE_DIR: ${{ github.workspace }}
          PROJECT_ROOT: /tmp/valdi_app

      - name: Refresh Bazel cache auth (pre-bootstrap_app)
        if: matrix.task == 'build-export'
        uses: ./.github/actions/bazel-cache-refresh
        with:
          gcp_project_id: ${{ vars.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ vars.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: "Run: Bootstrap app"
        if: matrix.task == 'build-export'
        run: |
          export PATH=~/.npm-global/bin:$PATH
          ./tools/ci/bootstrap_app.sh
        env:
          OPEN_SOURCE_DIR: ${{ github.workspace }}
          PROJECT_ROOT: /tmp/valdi_app

  snapshot-tests:
    name: Snapshot Tests
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure Bazel cache
        uses: ./.github/actions/bazel-cache
        with:
          gcp_project_id: ${{ vars.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ vars.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
          cache_bucket: ${{ vars.BAZEL_CACHE_BUCKET }}
          cache_key: snapshot-tests

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Bazel and watchman
        run: |
          brew install bazelisk watchman
          bazel --version

      - name: Run snapshot tests
        run: ./tools/ci/snapshot_tests.sh /tmp/valdi-snapshots

      - name: Upload diff images
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: snapshot-diffs
          path: /tmp/valdi-snapshots/diffs/
          retention-days: 5

  api-surface:
    name: API Surface Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Check API surface
        run: ./tools/ci/api_surface_tests.sh
